@interaction
let swap = (A: Participant, basket1: Asset1,
            B: Participant, basket2: Asset2) => {
  deposit! A -> basket1;
  deposit! B -> basket2;
  withdraw! B <- basket1;
  withdraw! A <- basket2;
}

// Usage example:
/*
swap(alice, {USD: 50}, bob, {EUR: 60});
*/
